streamlit==1.20.0          # Streamlit for building the web app
openai==0.27.0             # OpenAI API client for accessing GPT models
PyPDF2==1.26.0             # Library for reading PDF files
pydub==0.25.1              # Library for audio manipulation (optional, if needed)
numpy==1.23.5              # Numerical operations (optional, if needed)
requests==2.28.1           # Library for making HTTP requests
beautifulsoup4==4.11.1     # Library for parsing HTML and extracting text
urllib3>=2.0,<3.dev0       # Updated to resolve dependency conflict with gradio
